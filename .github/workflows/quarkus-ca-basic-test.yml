name: Basic Quarkus CA

on: workflow_call

jobs:
  # Smoke test for CA subsystem running on Quarkus
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Clone repository
        uses: actions/checkout@v4

      - name: Retrieve PKI images
        uses: actions/cache@v4
        with:
          key: pki-images-${{ github.sha }}
          path: pki-images.tar

      - name: Load PKI images
        run: docker load --input pki-images.tar

      - name: Verify CA Quarkus image structure
        run: |
          echo "Checking CA Quarkus image..."

          docker run --rm pki-quarkus-ca \
              test -f /usr/share/pki/quarkus/ca/quarkus-run.jar
          echo "  OK: quarkus-run.jar exists"

          docker run --rm pki-quarkus-ca \
              java -version
          echo "  OK: Java runtime available"

          docker run --rm pki-quarkus-ca \
              ls -la /usr/share/pki/quarkus/ca/
          echo "  OK: Quarkus app directory listing"
