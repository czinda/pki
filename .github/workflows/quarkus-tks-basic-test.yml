name: Basic Quarkus TKS

on: workflow_call

jobs:
  # Smoke test for TKS subsystem running on Quarkus
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Clone repository
        uses: actions/checkout@v4

      - name: Retrieve PKI images
        uses: actions/cache@v4
        with:
          key: pki-images-${{ github.sha }}
          path: pki-images.tar

      - name: Load PKI images
        run: docker load --input pki-images.tar

      - name: Verify TKS Quarkus image structure
        run: |
          echo "Checking TKS Quarkus image..."

          docker run --rm pki-quarkus-tks \
              test -f /usr/share/pki/quarkus/tks/quarkus-run.jar
          echo "  OK: quarkus-run.jar exists"

          docker run --rm pki-quarkus-tks \
              java -version
          echo "  OK: Java runtime available"

          docker run --rm pki-quarkus-tks \
              ls -la /usr/share/pki/quarkus/tks/
          echo "  OK: Quarkus app directory listing"
