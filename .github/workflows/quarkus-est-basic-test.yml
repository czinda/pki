name: Basic Quarkus EST

on: workflow_call

jobs:
  # Smoke test for EST subsystem running on Quarkus
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Clone repository
        uses: actions/checkout@v4

      - name: Retrieve PKI images
        uses: actions/cache@v4
        with:
          key: pki-images-${{ github.sha }}
          path: pki-images.tar

      - name: Load PKI images
        run: docker load --input pki-images.tar

      - name: Verify EST Quarkus image structure
        run: |
          echo "Checking EST Quarkus image..."

          docker run --rm pki-quarkus-est \
              test -f /usr/share/pki/quarkus/est/quarkus-run.jar
          echo "  OK: quarkus-run.jar exists"

          docker run --rm pki-quarkus-est \
              java -version
          echo "  OK: Java runtime available"

          docker run --rm pki-quarkus-est \
              ls -la /usr/share/pki/quarkus/est/
          echo "  OK: Quarkus app directory listing"
