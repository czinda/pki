name: Basic Quarkus ACME

on: workflow_call

jobs:
  # Smoke test for ACME subsystem running on Quarkus
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Clone repository
        uses: actions/checkout@v4

      - name: Retrieve PKI images
        uses: actions/cache@v4
        with:
          key: pki-images-${{ github.sha }}
          path: pki-images.tar

      - name: Load PKI images
        run: docker load --input pki-images.tar

      - name: Verify ACME Quarkus image structure
        run: |
          echo "Checking ACME Quarkus image..."

          docker run --rm pki-quarkus-acme \
              test -f /usr/share/pki/quarkus/acme/quarkus-run.jar
          echo "  OK: quarkus-run.jar exists"

          docker run --rm pki-quarkus-acme \
              java -version
          echo "  OK: Java runtime available"

          docker run --rm pki-quarkus-acme \
              ls -la /usr/share/pki/quarkus/acme/
          echo "  OK: Quarkus app directory listing"
